<div class="container">
      <%= form_for @chirp  do |f| %>
      <input type="hidden" value="" name="chirp[user_id]"/>
         <div class="form-group">
            <label for="chirp_title">Title</label>
            <input type="text" name="chirp[title]" id="user_nickname" class="form-control">
        </div>
          <div class="form-group">
            <label for="chirp_body">Content</label>
            <input type="text" name="chirp[body]" id="user_nickname" class="form-control">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      <%end%>
</div>